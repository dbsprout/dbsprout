# DBSprout configuration — generated by `dbsprout init`

[schema]
dialect = "sqlite"
source = "sqlite:///test.db"
snapshot = ".dbsprout/snapshots/883795e844088b9b.json"

[generation]
default_rows = 100
seed = 42
output_format = "sql"
output_dir = "./seeds"

# Per-table overrides (uncomment and customize):
# [tables.users]
# rows = 100
# [tables.orders]
# rows = 100
