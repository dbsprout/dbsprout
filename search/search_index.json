{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"DBSprout","text":"<p>Generate realistic seed data from your database schema.</p> <p>DBSprout reads your database schema (live connection or schema file), analyzes foreign key dependencies, and generates realistic seed data with 100% FK integrity. It works offline by default with an optional embedded AI model, supports SQLite, PostgreSQL, MySQL, and SQL Server, and accepts schemas from live databases, SQL DDL, DBML, Mermaid ERD, PlantUML, and Prisma files.</p>"},{"location":"#highlights","title":"Highlights","text":"<ul> <li>Schema-first \u2014 point at your database or a schema file, no config required.</li> <li>7 schema inputs \u2014 live DB or file (SQL DDL, DBML, Mermaid ERD, PlantUML, Prisma).</li> <li>100% FK integrity \u2014 topological ordering + FK sampling from parent PKs.</li> <li>Deterministic \u2014 same seed produces identical output for CI/testing.</li> <li>Offline by default \u2014 no internet, API key, or account required.</li> </ul>"},{"location":"#where-to-next","title":"Where to next","text":"<ul> <li>Install \u2014 get DBSprout onto your machine.</li> <li>Quick Start \u2014 install \u2192 init \u2192 generate in 5 minutes.</li> <li>CLI Reference \u2014 every command and option.</li> <li>Configuration \u2014 the <code>dbsprout.toml</code> reference.</li> <li>Architecture \u2014 the five-stage pipeline.</li> </ul>"},{"location":"contributing/","title":"Contributing","text":""},{"location":"contributing/#development-setup","title":"Development setup","text":"<pre><code>git clone https://github.com/dbsprout/dbsprout.git\ncd dbsprout\nuv sync --extra dev --extra data --extra stats\n</code></pre>"},{"location":"contributing/#quality-gates","title":"Quality gates","text":"<pre><code>uv run ruff check .                              # Lint\nuv run ruff format --check .                     # Formatting\nuv run mypy --strict dbsprout/                   # Type check\nuv run bandit -c pyproject.toml -r dbsprout/     # Security scan\nuv run pytest --cov=dbsprout                     # Tests (95% min coverage)\n</code></pre>"},{"location":"contributing/#working-on-the-docs-site","title":"Working on the docs site","text":"<pre><code>uv sync --extra docs\nuv run mkdocs serve            # Live preview at http://127.0.0.1:8000\nuv run mkdocs build --strict   # Same check CI and the test gate run\n</code></pre> <p>Documentation source lives in <code>site_docs/</code>; <code>mkdocs.yml</code> is at the repository root. The site deploys to GitHub Pages automatically on push to <code>main</code>.</p>"},{"location":"contributing/#writing-plugins","title":"Writing plugins","text":"<p>dbsprout is extensible via Python entry points. See the plugin development guide for the six extension points and a complete worked example.</p>"},{"location":"architecture/overview/","title":"Architecture Overview","text":"<p>DBSprout is a five-stage pipeline. Each stage is pluggable and the stages do not import one another.</p>"},{"location":"architecture/overview/#pipeline","title":"Pipeline","text":"<pre><code>SCHEMA INPUT --&gt; SPEC GENERATION --&gt; FK GRAPH --&gt; DATA GENERATION --&gt; VALIDATION --&gt; OUTPUT\n</code></pre> <ol> <li>Schema Input \u2014 reads the schema via live DB introspection    (SQLAlchemy for SQLite, PostgreSQL, MySQL, SQL Server) or file parsing    (SQL DDL, DBML, Mermaid ERD, PlantUML, Prisma) into a unified    <code>DatabaseSchema</code>.</li> <li>Spec Generation \u2014 heuristic regex/fuzzy matching (100+ patterns) or    an optional LLM analysis (one call per schema, cached) produces a    <code>DataSpec</code>.</li> <li>FK Graph \u2014 builds a dependency graph, detects cycles (Tarjan SCC),    and resolves them via nullable-FK deferral.</li> <li>Data Generation \u2014 Mimesis/NumPy value generation in topological    order, with FK columns sampled from parent primary keys (never    LLM-generated).</li> <li>Validation \u2014 FK satisfaction, PK uniqueness, UNIQUE and NOT NULL    checks; all must be 100%.</li> <li>Output \u2014 SQL INSERT (3 dialects), CSV, JSON, JSONL, with    insertion-order file numbering.</li> </ol>"},{"location":"architecture/overview/#hybrid-llm-as-architect","title":"Hybrid LLM-as-Architect","text":"<p>The LLM (when enabled) runs once per schema to produce a cached <code>DataSpec</code>. All row generation is deterministic via Mimesis/NumPy, so the same seed always reproduces the same data.</p>"},{"location":"architecture/overview/#plugin-system","title":"Plugin System","text":"<p>Parsers, generators, output writers, LLM providers, and migration frameworks are all discovered via Python entry points \u2014 every component is replaceable.</p>"},{"location":"contributing/plugins/","title":"Writing plugins","text":"<p>dbsprout is extensible through Python entry points. Every pipeline stage \u2014 schema parsing, spec generation, row generation, output, migration detection, and training extraction \u2014 can be replaced or extended by a third-party package, with no changes to dbsprout itself.</p>"},{"location":"contributing/plugins/#how-discovery-works","title":"How discovery works","text":"<p>On first use dbsprout builds a process-wide registry by enumerating the six entry-point groups below. For each entry point it imports the target object and runs an <code>isinstance</code> check against the group's <code>@runtime_checkable</code> Protocol.</p> <p>Because that check inspects class-level attributes, declare Protocol data attributes (<code>suffixes</code>, <code>format</code>) on the class, not inside <code>__init__</code>. Plugins that fail the Protocol check are recorded with <code>status=\"error\"</code> and skipped at dispatch; entry points that fail to import are logged and dropped. Inspect the result with:</p> <pre><code>dbsprout plugins list\ndbsprout plugins check dbsprout.parsers:yaml\n</code></pre> <p>Register an instance for parsers</p> <p><code>dbsprout.parsers</code> dispatch (<code>parse_schema_file</code>) calls <code>can_parse</code> and <code>parse</code> directly on the registered object without instantiating it, and the registry's <code>isinstance</code> check needs a real instance. Point the parser entry point at a module-level instance (the in-tree adapters do this), e.g. <code>pkg:yaml_parser</code>, not the class <code>pkg:YamlParser</code>. Generator and output entry points, by contrast, are instantiated by the dispatcher, so they point at the class.</p>"},{"location":"contributing/plugins/#entry-point-groups-and-protocols","title":"Entry-point groups and Protocols","text":"Group Protocol Required interface <code>dbsprout.parsers</code> <code>SchemaParser</code> <code>suffixes: tuple[str, ...]</code>; <code>can_parse(text: str) -&gt; bool</code>; <code>parse(text: str, *, source_file: str \\| None = None) -&gt; DatabaseSchema</code> <code>dbsprout.generators</code> <code>GenerationEngine</code> <code>generate_table(*args, **kwargs) -&gt; list[dict[str, Any]]</code> <code>dbsprout.outputs</code> <code>OutputWriter</code> <code>format: str</code>; <code>write(*args, **kwargs) -&gt; Any</code> <code>dbsprout.llm_providers</code> <code>SpecProvider</code> <code>generate_spec(schema: DatabaseSchema) -&gt; DataSpec</code> <code>dbsprout.migration_frameworks</code> <code>MigrationParser</code> <code>detect_changes(project_path: Path) -&gt; list[SchemaChange]</code> <code>dbsprout.train_extractors</code> <code>TrainExtractor</code> <code>extract(*, source: str, config: ExtractorConfig) -&gt; SampleResult</code>"},{"location":"contributing/plugins/#schemaparser","title":"SchemaParser","text":"<p><code>suffixes</code> is the tuple of file extensions the parser claims (e.g. <code>(\".yaml\", \".yml\")</code>). <code>can_parse(text)</code> returns whether the parser can handle a given file's contents \u2014 return <code>False</code> (never raise) so dispatch can fall through to another parser. <code>parse(text, *, source_file=None)</code> returns a populated <code>DatabaseSchema</code>; raise <code>ValueError</code> on malformed input.</p>"},{"location":"contributing/plugins/#generationengine","title":"GenerationEngine","text":"<p><code>generate_table</code> returns a list of row dicts for one table. The concrete signature varies across in-tree engines (some take a column\u2192generator mapping, others a per-table <code>DataSpec</code>); the Protocol only fixes the method name and return type, so callers pass the kwargs the engine documents. Engine entry points point at the class \u2014 the dispatcher instantiates it with <code>seed=...</code>.</p>"},{"location":"contributing/plugins/#outputwriter","title":"OutputWriter","text":"<p><code>format</code> is the format key users select (e.g. <code>\"csv\"</code>). <code>write</code> persists generated rows for a full schema; accept the kwargs you need and ignore the rest. Output entry points point at the class \u2014 the dispatcher instantiates it with no arguments.</p>"},{"location":"contributing/plugins/#specprovider","title":"SpecProvider","text":"<p><code>generate_spec(schema)</code> returns a <code>DataSpec</code> describing per-column generators, distributions, and correlations. Providers run once per schema and the result is cached.</p>"},{"location":"contributing/plugins/#migrationparser","title":"MigrationParser","text":"<p><code>detect_changes(project_path)</code> inspects a migration framework's project directory and returns the list of <code>SchemaChange</code> objects describing what changed since the last seed.</p>"},{"location":"contributing/plugins/#trainextractor","title":"TrainExtractor","text":"<p><code>extract(*, source, config)</code> pulls a stratified <code>SampleResult</code> from a data source for fine-tuning. <code>source</code> is a plain string (URL, path, \u2026), so new extractors (CSV dump, Parquet dir, \u2026) need no new Protocol.</p>"},{"location":"contributing/plugins/#example-a-yaml-schema-parser","title":"Example: a YAML schema parser","text":"<p>A complete, runnable example lives in <code>examples/plugin-yaml-parser/</code>. It registers a <code>SchemaParser</code> for <code>.yaml</code>/<code>.yml</code> files.</p>"},{"location":"contributing/plugins/#1-declare-the-entry-point","title":"1. Declare the entry point","text":"<p><code>pyproject.toml</code>:</p> <pre><code>[project]\nname = \"dbsprout-plugin-yaml\"\ndependencies = [\"dbsprout\", \"pyyaml&gt;=6.0\"]\n\n[project.entry-points.\"dbsprout.parsers\"]\nyaml = \"dbsprout_plugin_yaml:yaml_parser\"\n</code></pre> <p>The key (<code>yaml</code>) is the plugin name shown by <code>dbsprout plugins list</code>; the value is <code>module:attribute</code> and resolves to a module-level <code>YamlParser()</code> instance.</p>"},{"location":"contributing/plugins/#2-implement-the-protocol","title":"2. Implement the Protocol","text":"<pre><code>class YamlParser:\n    suffixes: tuple[str, ...] = (\".yaml\", \".yml\")\n\n    def can_parse(self, text: str) -&gt; bool:\n        ...  # True only when text is a YAML schema mapping\n\n    def parse(\n        self, text: str, *, source_file: str | None = None\n    ) -&gt; DatabaseSchema:\n        ...  # build and return a DatabaseSchema\n\n\nyaml_parser = YamlParser()  # the entry point points here\n</code></pre> <p><code>suffixes</code> is class-level so the registry's <code>isinstance</code> check passes.</p>"},{"location":"contributing/plugins/#3-test-it","title":"3. Test it","text":"<p>The example ships unit tests (<code>examples/plugin-yaml-parser/tests/test_parser.py</code>) and dbsprout's own test suite loads it through the real registry and drives it end-to-end via <code>parse_schema_file</code> (<code>tests/integration/test_plugins/test_example_yaml_plugin.py</code>).</p>"},{"location":"contributing/plugins/#4-install-and-verify","title":"4. Install and verify","text":"<pre><code>pip install -e examples/plugin-yaml-parser\ndbsprout plugins list\ndbsprout plugins check dbsprout.parsers:yaml\ndbsprout init --file schema.yaml\n</code></pre> <p>Once installed the parser is auto-discovered: any <code>.yaml</code>/<code>.yml</code> schema file passed to <code>dbsprout init</code> / <code>generate</code> / <code>diff</code> is routed to it.</p>"},{"location":"getting-started/install/","title":"Install","text":"<p>Requirements: Python 3.10+</p> <pre><code>pip install dbsprout\n</code></pre> <p>Or with uv:</p> <pre><code>uv add dbsprout\n</code></pre>"},{"location":"getting-started/install/#optional-extras","title":"Optional extras","text":"<pre><code>pip install dbsprout[db]       # SQLAlchemy + drivers (PostgreSQL, MySQL)\npip install dbsprout[mssql]    # SQL Server support (pyodbc)\npip install dbsprout[llm]      # Embedded LLM (llama-cpp-python + Qwen 2.5-1.5B)\npip install dbsprout[cloud]    # Cloud LLM providers (LiteLLM + Instructor)\npip install dbsprout[privacy]  # PII redaction (Presidio)\npip install dbsprout[dev]      # Development tools (pytest, ruff, mypy)\npip install dbsprout[docs]     # Documentation site tooling (mkdocs-material)\n</code></pre> <p>The core install is dependency-light; everything beyond schema parsing and heuristic generation is an opt-in extra.</p>"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>From zero to seed data in under five minutes.</p>"},{"location":"getting-started/quick-start/#1-install","title":"1. Install","text":"<pre><code>pip install dbsprout\n</code></pre>"},{"location":"getting-started/quick-start/#2-initialize-from-your-database","title":"2. Initialize from your database","text":"<pre><code># SQLite\ndbsprout init --db sqlite:///myapp.db\n\n# PostgreSQL\ndbsprout init --db postgresql://user:pass@localhost:5432/mydb\n\n# MySQL\ndbsprout init --db mysql+pymysql://user:pass@localhost:3306/mydb\n</code></pre> <p>Or from a schema file:</p> <pre><code>dbsprout init --file schema.sql       # SQL DDL\ndbsprout init --file schema.dbml      # DBML\ndbsprout init --file schema.mmd       # Mermaid ERD\ndbsprout init --file schema.puml      # PlantUML\ndbsprout init --file schema.prisma    # Prisma\n</code></pre> <p><code>init</code> introspects the schema, writes a <code>dbsprout.toml</code>, and stores a schema snapshot under <code>.dbsprout/</code>.</p>"},{"location":"getting-started/quick-start/#3-generate-seed-data","title":"3. Generate seed data","text":"<pre><code># Default heuristic engine: 100 rows/table, SQL INSERT, ./seeds/\ndbsprout generate\n\n# 500 rows per table as CSV\ndbsprout generate --rows 500 --output-format csv\n\n# Reproducible JSON output with an explicit seed\ndbsprout generate --output-format json --seed 123\n</code></pre> <p>Output files are numbered by insertion order so they apply cleanly:</p> <pre><code>seeds/\n  001_authors.sql\n  002_books.sql\n  003_orders.sql\n</code></pre>"},{"location":"getting-started/quick-start/#4-validate-integrity","title":"4. Validate integrity","text":"<pre><code>dbsprout validate\n</code></pre> <p>DBSprout validates FK satisfaction, PK uniqueness, UNIQUE and NOT NULL constraints \u2014 all must be 100%.</p> <p>Deterministic by default</p> <p>The same <code>--seed</code> always produces identical output, which makes DBSprout safe to use inside CI pipelines and reproducible test fixtures.</p>"},{"location":"user-guide/cli/","title":"CLI Reference","text":"<p>Run <code>dbsprout --help</code> for the live command list, or <code>dbsprout &lt;command&gt; --help</code> for any command's options.</p>"},{"location":"user-guide/cli/#dbsprout-init","title":"<code>dbsprout init</code>","text":"<p>Introspect a database schema and generate configuration.</p> <pre><code>dbsprout init --db &lt;connection-url&gt;    # From a live database\ndbsprout init --file &lt;schema-file&gt;     # From a schema file (.sql, .dbml, .mmd, .puml, .prisma)\ndbsprout init --dry-run                # Preview without writing files\ndbsprout init --output-dir ./config    # Custom output directory\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-generate","title":"<code>dbsprout generate</code>","text":"<p>Generate seed data from the schema snapshot.</p> <pre><code>dbsprout generate                              # Defaults: 100 rows, SQL, ./seeds/\ndbsprout generate --rows 500                   # 500 rows per table\ndbsprout generate --seed 123                   # Deterministic output\ndbsprout generate --output-format csv          # CSV output\ndbsprout generate --output-format json         # JSON (pretty-printed array)\ndbsprout generate --output-format jsonl        # JSONL (one object per line)\ndbsprout generate --dialect mysql              # MySQL SQL dialect\ndbsprout generate --output-dir ./my-seeds      # Custom output directory\ndbsprout generate --engine spec                # AI-powered spec engine\ndbsprout generate --privacy cloud              # Allow cloud LLM providers\ndbsprout generate --schema-snapshot path.json  # Explicit schema path\ndbsprout generate --config dbsprout.toml       # Explicit config path\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-validate","title":"<code>dbsprout validate</code>","text":"<p>Validate integrity of generated seed data.</p> <pre><code>dbsprout validate                              # Validate with defaults\ndbsprout validate --rows 500                   # Validate 500 rows per table\ndbsprout validate --engine spec                # Validate spec-driven output\ndbsprout validate --format json                # JSON output (for CI pipelines)\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-diff","title":"<code>dbsprout diff</code>","text":"<p>Report schema changes since the last snapshot.</p> <pre><code>dbsprout diff                                  # Show schema changes vs the stored snapshot\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-audit","title":"<code>dbsprout audit</code>","text":"<p>Show the LLM interaction audit log.</p> <pre><code>dbsprout audit                                 # Show all audit entries\ndbsprout audit --last 10                       # Show 10 most recent entries\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-models","title":"<code>dbsprout models</code>","text":"<p>List, download, and inspect embedded GGUF models.</p> <pre><code>dbsprout models                                # Inspect embedded GGUF models\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-plugins","title":"<code>dbsprout plugins</code>","text":"<p>Inspect discovered dbsprout plugins.</p> <pre><code>dbsprout plugins                               # List discovered plugins\n</code></pre>"},{"location":"user-guide/cli/#dbsprout-train","title":"<code>dbsprout train</code>","text":"<p>Training pipeline subcommands (QLoRA fine-tuning).</p> <pre><code>dbsprout train --help                          # Show training subcommands\n</code></pre>"},{"location":"user-guide/configuration/","title":"Configuration","text":"<p>DBSprout is configured by a <code>dbsprout.toml</code> file, normally generated by <code>dbsprout init</code>. All keys are validated \u2014 unknown keys are rejected with a clear error.</p>"},{"location":"user-guide/configuration/#example","title":"Example","text":"<pre><code>[schema]\ndialect = \"postgresql\"\nsource = \"postgresql://user:***@localhost:5432/mydb\"\nsnapshot = \".dbsprout/snapshots/a1b2c3d4.json\"\n\n[generation]\ndefault_rows = 100\nseed = 42\nengine = \"heuristic\"\noutput_format = \"sql\"\noutput_dir = \"./seeds\"\n\n[privacy]\ntier = \"local\"\n\n# Per-table overrides\n[tables.users]\nrows = 50\n\n[tables.audit_logs]\nexclude = true\n</code></pre>"},{"location":"user-guide/configuration/#schema","title":"<code>[schema]</code>","text":"Key Type Default Notes <code>dialect</code> string unset SQL dialect (<code>sqlite</code>, <code>postgresql</code>, <code>mysql</code>, \u2026). <code>source</code> string unset Connection URL or schema file path. <code>snapshot</code> string unset Path to the stored schema snapshot."},{"location":"user-guide/configuration/#generation","title":"<code>[generation]</code>","text":"Key Type Default Notes <code>default_rows</code> int <code>100</code> Rows per table (&gt;= 1). <code>seed</code> int <code>42</code> Deterministic seed (&gt;= 0). <code>engine</code> string <code>heuristic</code> One of <code>heuristic</code>, <code>spec</code>, <code>statistical</code>, <code>finetuned</code>. <code>output_format</code> string <code>sql</code> One of <code>sql</code>, <code>csv</code>, <code>json</code>, <code>jsonl</code>, <code>parquet</code>. <code>output_dir</code> string <code>./seeds</code> Output directory for generated files."},{"location":"user-guide/configuration/#privacy","title":"<code>[privacy]</code>","text":"Key Type Default Notes <code>tier</code> string <code>local</code> One of <code>local</code>, <code>redacted</code>, <code>cloud</code>."},{"location":"user-guide/configuration/#tablesname","title":"<code>[tables.&lt;name&gt;]</code>","text":"<p>Per-table overrides. Each section is keyed by table name.</p> Key Type Default Notes <code>rows</code> int unset Override row count for this table. <code>exclude</code> bool <code>false</code> Skip this table during generation."}]}